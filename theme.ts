import {DefaultTheme} from 'react-native-paper';

const theme = {
	...DefaultTheme,
	colors: {
		...DefaultTheme.colors,
		primary: '#efc410',
		secondary: '#223046',
		background: '#F5F5F5',
	},
};

export default theme;
