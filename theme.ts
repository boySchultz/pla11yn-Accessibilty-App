import {DefaultTheme} from 'react-native-paper';

const theme = {
	...DefaultTheme,
	colors: {
		...DefaultTheme.colors,
		primary: '#ffd34d',
		secondary: '#2E3A57',
		background: '#F5F5F5',
	},
};

export default theme;
